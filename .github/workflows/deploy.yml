name: DXL API deployment

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Deploy
      run: |
        ssh -i ${{ secrets.SSH_PRIVATE_KEY }} danishxboxleagu.dk@linux279.unoeuro.com << EOF
        cd staging/wp-content/plugins/dxl-api
        git pull
        EOF

